const ProfileHeader = ( { username } ) => {
    return (
        <header class="center">
            <h4 class="textCenter">{username}, Welcome Back</h4>
        </header>
    )
}

export default ProfileHeader